# srev — source reviewer

**日本語** | [English](README.md)

コードを「読む」ことに特化したターミナル TUI ビューワーです。
差分の確認とフルファイルの閲覧に集中できるよう、操作をシンプルにまとめています。編集機能はありません。

---

## 特長

- **作業ツリー vs HEAD の差分**を `d` で表示（未コミットの変更をレビュー）
- **差分 ⇄ フルコードのトグル**（`d`）。見ている行を保持したまま相互にジャンプ
- **vim ライクなカーソル**でコードを読み、カーソル下の語から**定義へジャンプ**（`gd`）
- **参照検索**（`gr`）。カーソル下のシンボルの使用箇所をプロジェクト全体から一覧表示してジャンプ
- **LSP による `gd` / `gr`**：言語サーバーが利用可能なら（既定で rust-analyzer / intelephense / ruby-lsp、設定で変更可）意味解析ベースの正確なジャンプを行う。サーバーが無い／起動中は、内蔵の tree-sitter タグ索引へ自動フォールバック
- **ジャンプ履歴**：`gd`・アウトライン・プロジェクト検索・`gg`/`G`・ファイル内検索などのジャンプで「飛ぶ前の位置」を記憶し、`(` / `)` で戻る/進む（ファイルをまたいでも可）。**右側のジャンプ履歴ペイン**に経路を表示し、現在地を強調（`J` で表示切替）
- **visual mode**（`v`/`V`）で範囲選択し、`y` でクリップボードへコピー
- **左下アウトラインペイン**にシンボル一覧を表示して定義へ移動できる。サーバーがあれば **LSP の `documentSymbol`**（例: PHP は intelephense）、無ければ tree-sitter 索引から取得し、タイトルに取得元を表示（`Symbols · LSP` / `Symbols · tree-sitter`）
- **ファイル内検索**（`/` → `Enter`、`n`/`N` で前後のマッチへ）
- **あいまいファイル検索**（`Ctrl-P`、nucleo = fzf 相当）
- **プロジェクト全体の本文検索**（`Ctrl-F`、部分一致・大文字小文字無視）でマッチ行へジャンプ
- **ツリー / アウトライン / オーバーレイの `/`** で入力しながら候補を絞り込み
- **`Ctrl-R` で pull＋リロード**：`origin` の同名ブランチを pull してから、開いているファイル・git 状態・ツリー・索引を再読込（カーソル位置保持）。upstream 無し/オフライン時は従来のローカル reload にフォールバック。コンフリクト時は merge を中止
- **merge ピッカー**（`m`）：`origin` を fetch し、ローカル/リモートの任意ブランチを**文字入力であいまい絞り込み**して現在ブランチへ merge。コンフリクト時は自動で中止し、外部エディタでの解決を促す（srev に編集機能は無い）
- **多言語シンタックスハイライト**（syntect、200+ 言語。PHP・HAML・TOML・TypeScript 等）。**Markdown はコードフェンス内の言語別ハイライトにも対応**
- **差分レビュー操作**：`n`/`N` または `}`/`{` で変更ブロック間ジャンプ、`]`/`[` で変更ファイル間を移動、`h`/`l`（`0`/`$` で行頭・右端）で長い行を横スクロール。右端の**変更オーバービュー・バー**が変更箇所と現在の表示範囲を俯瞰表示。差分は**既定で左右（side-by-side）**表示（新規/削除ファイルは自動で単一表示）、`s` で単一表示と切替
- **コミットビュー**（`c`）：左ペインにデフォルトブランチとの差分コミット（`default..HEAD`）を一覧表示（デフォルトブランチ上なら全履歴）。コミットを選ぶと変更ファイル（左下）と選択ファイルの左右差分が見られる
- **PR 差分ビュー**（`C`）：ブランチ全体を 1 つの変更セットとしてレビュー。左ペインにデフォルトブランチとの全変更ファイルを一覧（three-dot `merge-base(default, HEAD)..HEAD`。分岐後にデフォルト側へ入ったコミットは除外）、右に各ファイルの全文脈差分を表示。`}`/`{` や `n`/`N` で変更箇所を辿れる
- **ブランチ切替**（`Ctrl-V`）：先に `origin` を fetch し、ローカル／リモートのブランチ一覧を表示（**文字入力であいまい絞り込み**）。選択で checkout（`git` 経由。リモートは追跡ブランチを自動作成）し、自動でリロード。**現在ブランチ**はステータスバー右端にチップで常時表示
- ファイルツリーに変更ステータスを表示（`M`=変更 / `A`=追加 / `D`=削除 / `?`=未追跡）。ドットファイルや `.gitignore` 対象ファイルも**淡色**で一覧表示。無視対象ディレクトリ（`target/` 等）は表示するが中までは展開しない。`.git` ディレクトリは除外
- **コードビューでも HEAD との変更行を gutter に表示**（追加=緑 / 変更=青 / 直前に削除あり=赤、エディタ風）。`}`/`{`（検索していないときは `n`/`N` でも）で変更ブロック間をジャンプでき、右端のオーバービュー・バーにも位置を表示
- 単一バイナリ。Linux / macOS / Windows でビルド可能

---

## インストール

Rust ツールチェーン（`cargo`）と **C コンパイラ**が必要です（libgit2 と
tree-sitter 文法のビルドに使用）。release バイナリは約 10 MB です。

### crates.io から（推奨）

```sh
cargo install srev
```

### GitHub から（最新の開発版）

```sh
cargo install --git https://github.com/ore-public/srev
```

いずれもバイナリは `~/.cargo/bin/srev` に置かれます（`$PATH` が通っていれば
`srev` で起動できます）。

### ソースからビルド（開発用）

```sh
git clone https://github.com/ore-public/srev
cd srev
cargo build --release   # 出力: target/release/srev
cargo test              # ユニットテスト一式
```

---

## 使い方

### 起動

```sh
srev [PATH]   # PATH 省略時はカレントディレクトリ
```

- 起動時は**コードモード**（ファイルツリー閲覧）です。`d` で差分（作業ツリー vs HEAD）に切り替えます。

### 画面構成

```
┌──────────────┬──────────────────────────────┐
│ ファイルツリー │                              │
│（コードモード）│         本文                  │
│  変更ファイル  │   （コード または 差分）        │
│（差分モード）  │                              │
├──────────────┤                              │
│  アウトライン  │                              │
│  (Symbols)   │                              │
└──────────────┴──────────────────────────────┘
                  ステータス / ヘルプ行
```

- **左上**：コードモード時はファイルツリー、差分モード時は変更ファイル一覧
- **左下**：開いているファイルのシンボル一覧（Symbols）
- **右**：本文（コードまたは unified diff）
- **下部**：ステータス行とキーヒント

`d` キーでコード ⇄ 差分をトグルできます（見ている行に対応する位置を保持）。

### キーバインド

#### 共通

| キー | 動作 |
|------|------|
| `q` | 終了 |
| `Tab` | フォーカス巡回（ツリー → アウトライン → 本文） |
| `d` | 差分 ⇄ コードを切替（見ている行を保持） |
| `c` | コミットビューを切替（デフォルトブランチとの差分コミット、または全履歴） |
| `C` | PR 差分ビューを切替（デフォルトブランチとの全変更ファイル。three-dot 差分） |
| `Ctrl-V` | ブランチ切替（先に `origin` を fetch → 一覧から選んで checkout） |
| `m` | merge ピッカー（`origin` を fetch → ローカル/リモートのブランチを選んで現在ブランチへ merge） |
| `Ctrl-P` | ファイル名あいまい検索オーバーレイを開く |
| `Ctrl-F` | プロジェクト全体の本文検索（部分一致）。`Enter` で該当行へジャンプ |
| `Ctrl-R` | `origin` の同名ブランチを pull → reload。upstream 無し/オフライン時は従来のローカル reload にフォールバック。コンフリクト時は merge を中止 |
| `]` / `[` | 次 / 前のファイルを開く（コードモード=全ファイル昇順、差分モード=変更ファイル） |
| `(` / `)` | ジャンプ履歴を戻る / 進む（`Ctrl-O` も戻る） |
| `J` | 右側のジャンプ履歴ペインの表示切替（既定で表示） |

#### ツリー（コードモード）/ 変更ファイル一覧（差分モード）

| キー | 動作 |
|------|------|
| `j` / `k`、`↓` / `↑` | 移動 |
| `Enter` / `l` | ファイルを開く / ディレクトリを展開 |
| `h` | ディレクトリを畳む（ツリー時のみ） |
| `/` | インラインあいまい絞り込み |

#### アウトライン（左下）

| キー | 動作 |
|------|------|
| `j` / `k` | シンボル選択 |
| `Enter` / `l` | その定義行へカーソル移動 |
| `/` | シンボルを絞り込み |

#### 本文（コードモード）

| キー | 動作 |
|------|------|
| `h` / `j` / `k` / `l` | カーソル移動 |
| `w` / `b` | 単語単位で前後（行内のみ） |
| `0` / `$` | 行頭 / 行末 |
| `gg` / `G` | ファイル先頭 / 末尾 |
| `Ctrl-d` / `Ctrl-u` | 半画面スクロール |
| `gd` | カーソル下の語の定義へジャンプ |
| `gr` | カーソル下の語の参照（呼び出し元）一覧。`Enter` でジャンプ（`↑`/`↓` または `Ctrl-n`/`Ctrl-p` で移動、`Esc` で閉じる） |
| `v` / `V` | visual mode 開始（文字単位 / 行単位） |
| `y` | 選択範囲をクリップボードへコピー |
| `Y` | 位置情報をコピー（選択なし=`パス:行:列`／単一行選択=`パス:行`／複数行選択=`パス:開始行-終了行`） |
| `Esc` | 選択解除 |
| `/` → 入力 → `Enter` | ファイル内検索 |
| `n` / `N` | 次 / 前の検索マッチへ。検索していないときは次 / 前の変更ブロックへジャンプ |
| `}` / `{` | 次 / 前の変更ブロック（HEAD との差分）へジャンプ |

> `gg`・`gd`・`gr` の `g` プレフィックスは固定で、設定ファイルによる変更対象外です。

#### 本文（差分モード）

| キー | 動作 |
|------|------|
| `j` / `k` | スクロール |
| `h` / `l`、`0` / `$` | 長い行の横スクロール（`0`=行頭、`$`=右端） |
| `gg` / `G` | 先頭 / 末尾へ |
| `PageDown` / `PageUp` | ページスクロール |
| `n` / `N`、`}` / `{` | 次 / 前の変更ブロックへジャンプ |
| `s` | side-by-side ⇄ unified の切替（既定は side-by-side。新規/削除ファイルは自動で単一表示） |

#### オーバーレイ / フィルタ / 検索入力中

| キー | 動作 |
|------|------|
| `Esc` | 取消・閉じる |
| `Enter` | 確定 |
| `Backspace` | 入力を編集 |
| `↑` / `↓` または `Ctrl-p` / `Ctrl-n` | 候補を上下移動 |

> `Ctrl-p` / `Ctrl-n` による候補移動は**フィルタ・オーバーレイ入力中のみ**有効です。
> 通常モードの `Ctrl-P`（大文字）はファイル検索オーバーレイを開くキーです。

---

## 設定

### キーバインドの変更

設定ファイル: `~/.config/srev/config.toml`（`SREV_CONFIG` 環境変数で別パスを指定可）

`[keys]` テーブルに `"キー" = "アクション名"` を記述します。

```toml
[keys]
"ctrl-r" = "reload"
"x"      = "toggle_diff"   # 別キーを割り当て
"d"      = "none"          # 既定の d を無効化
```

### キー表記

| 種類 | 例 |
|------|----|
| 単一文字 | `"a"`, `"/"`, `"$"` |
| Ctrl 修飾 | `"ctrl-p"`, `"ctrl-r"` |
| 大文字 | `"Y"`, `"G"`, `"N"` |
| 特殊キー名 | `tab`, `enter`, `esc`, `space`, `up`, `down`, `left`, `right`, `home`, `end`, `pageup`, `pagedown`, `backspace`, `del` |

### アクション名一覧

| アクション名 | 説明 |
|-------------|------|
| `quit` | 終了 |
| `focus_next` | フォーカス巡回 |
| `down` | 下移動 |
| `up` | 上移動 |
| `left` | 左移動 / ディレクトリを畳む |
| `right` | 右移動 / 開く |
| `activate` | 開く / 確定 |
| `top` | 先頭へ（`gg` 相当） |
| `bottom` | 末尾へ（`G` 相当） |
| `half_page_down` | 半画面下スクロール |
| `half_page_up` | 半画面上スクロール |
| `word_forward` | 単語前進 |
| `word_back` | 単語後退 |
| `line_start` | 行頭 |
| `line_end` | 行末 |
| `toggle_diff` | 差分 ⇄ コード切替 |
| `goto_def` | 定義へジャンプ |
| `goto_references` | カーソル下シンボルの参照（呼び出し元）一覧 |
| `find` | ファイル内検索開始 |
| `search_next` | 次のマッチ |
| `search_prev` | 前のマッチ |
| `visual_char` | 文字単位 visual mode |
| `visual_line` | 行単位 visual mode |
| `yank` | 選択をコピー |
| `yank_location` | 位置をコピー（選択時は行/行範囲） |
| `fuzzy_find` | ファイル検索オーバーレイ |
| `reload` | リロード |
| `cancel` | 選択解除 / キャンセル |
| `next_file` | 次のファイルを開く（コード=全ファイル / 差分=変更ファイル） |
| `prev_file` | 前のファイルを開く（コード=全ファイル / 差分=変更ファイル） |
| `toggle_split` | unified ⇄ side-by-side 切替 |
| `grep` | プロジェクト全体の本文検索 |
| `jump_back` | ジャンプ履歴を戻る |
| `jump_forward` | ジャンプ履歴を進む |
| `toggle_jumps` | ジャンプ履歴ペインの表示切替 |
| `toggle_commits` | コミットビューの表示切替 |
| `toggle_branch_diff` | PR 差分ビューの表示切替（デフォルトブランチとの差分） |
| `next_change` | 次の変更ブロックへジャンプ |
| `prev_change` | 前の変更ブロックへジャンプ |
| `switch_branch` | ブランチ切替ピッカーを開く（fetch ＋ checkout） |
| `merge_branch` | merge ピッカーを開く（fetch ＋ 現在ブランチへ merge） |

`"none"` を指定するとそのキーを無効化できます。
`gg` / `gd` の `g` プレフィックスは設定変更の対象外です。

### 言語サーバー（LSP）

`gd` / `gr` は言語サーバーが利用可能ならそれを使い、無い場合は tree-sitter タグ索引に
フォールバックします（[コードジャンプ](#コードジャンプgd--grとアウトライン)参照）。

**組み込みデフォルト**（バイナリが `$PATH` にあればそのまま使われます）:

| 言語 | 拡張子 | 既定コマンド | タグ・フォールバック |
|------|--------|--------------|----------------------|
| Rust | `rs` | `rust-analyzer` | あり |
| PHP | `php`, `phtml` | `intelephense --stdio` | **なし** |
| Ruby | `rb`, `rake`, `gemspec` | `ruby-lsp` | あり |

> PHP はタグ・フォールバックが無いため、`gd` / `gr` には起動中のサーバーが必須です。
> 使うサーバーを導入し `$PATH` に通してください（例：
> `rustup component add rust-analyzer`、`npm i -g intelephense`、`gem install ruby-lsp`）。

**設定**：`~/.config/srev/config.toml`（キーマップ設定と共用）や、優先される
リポジトリ直下の `<project-root>/.srev.toml` に `[lsp.<id>]` テーブルを追加します。
各エントリで対象 `extensions` と起動 `command` を指定します。デフォルトの上書き、
**再コンパイル無しでの新言語追加**、空コマンドでの無効化ができます。

```toml
# Ruby サーバーを差し替え
[lsp.ruby]
extensions = ["rb", "rake", "gemspec"]
command = ["solargraph", "stdio"]   # 既定の ruby-lsp の代わりに

# PHP サーバーを別のものに
[lsp.php]
extensions = ["php", "phtml"]
command = ["phpactor", "language-server"]

# 組み込みに無い言語を追加（リビルド不要）
[lsp.go]
extensions = ["go"]
command = ["gopls"]

# Rust の LSP を無効化（タグ索引にフォールバック）
[lsp.rust]
command = []
```

補足:

- srev は**読み取り専用**なので `textDocument/didOpen` しか送りません（サーバーが
  ファイルを書き換えることはありません）。
- サーバーはその言語で最初に `gd` / `gr` した時に遅延起動し、srev 終了時に停止します。
- ファイルを開いた直後の最初の `gd` は、サーバーがまだ索引中だとタグ索引に
  フォールバックすることがあります。準備完了後にもう一度押してください。

---

## 対応言語

### シンタックスハイライト

[syntect](https://github.com/trishume/syntect) と拡張セット
[two-face](https://github.com/CosmicHorrorDev/two-face) により **200+ 言語**に対応
（PHP・**HAML**・TOML・TypeScript・Vue・Svelte 等）。`.blade.php` と `.phtml` は
PHP として色付けします（Blade 専用文法は同梱なし）。
**Markdown** はコードフェンス（```rust など）の中身も言語別にハイライトします。

### コードジャンプ（`gd` / `gr`）とアウトライン

`gd` / `gr` は 2 段階で解決します:

1. **LSP（優先）** — 言語サーバーが導入され `$PATH` にあれば、意味解析ベースの正確な
   ジャンプを行います（同名シンボル・import・スコープを区別）。組み込みデフォルトと
   設定方法は [言語サーバー（LSP）](#言語サーバーlsp) を参照。
2. **tree-sitter タグ索引（フォールバック）** — サーバーが無い／起動中に使用。
   名前ベースの近似で、**Rust / Python / JavaScript / Go / Ruby / C** に対応。

**アウトラインペイン（Symbols）**も同様で、開いた直後は tree-sitter のシンボルを即時表示し、
言語サーバーの準備が整うと **`documentSymbol` に差し替え**ます（PHP など tree-sitter 非対応の
言語でもアウトラインが出ます）。ペインのタイトルに取得元を表示します — `Symbols · tree-sitter`
または `Symbols · LSP`。

> アウトラインがサーバーを使うため、LSP 設定済み言語のファイルを開くと**その時点で
> サーバーがバックグラウンド起動**します（以前は最初の `gd` / `gr` 時に起動）。`gd` / `gr` の
> ウォームアップにもなります。したがって Rust ファイルを閲覧するだけで rust-analyzer が
> 立ち上がります。サーバー未設定ならアウトラインは tree-sitter のままです。

---

## 技術スタック

| 役割 | クレート |
|------|----------|
| TUI / 端末抽象 | `ratatui` + `crossterm` |
| シンタックスハイライト | `syntect` + `two-face`（200+ 言語） |
| あいまい検索 | `nucleo-matcher` |
| ファイル走査（gitignore 準拠） | `ignore` |
| git 差分 / 状態 | `git2`（vendored libgit2） |
| シンボル索引 / 定義ジャンプ（フォールバック） | `tree-sitter-tags` |
| `gd` / `gr` の LSP クライアント | `lsp-types` + `serde_json`（stdio 上の JSON-RPC） |
| クリップボード | `arboard` |
| 設定（キーマップ＋LSP） | `toml` |

---

## 既知の制限

- **横スクロール未対応**。長い行はカーソルでたどれますが、画面は横にスクロールしません。
- **side-by-side 表示**では各ペインが画面幅の約半分になるため、長い行はより早くクリップされます。
  また `s` でのトグル時、スクロール位置は厳密には対応せず近い位置に再表示されます。
- **`gd` の索引はバックグラウンド構築**。起動直後に作り始めるため、大規模プロジェクトで
  起動直後に `gd` すると「インデックス構築中…」と表示されることがあります（完了後は即ジャンプ）。
- **全角文字 / タブを含む行**：カーソルおよび選択ハイライトの表示位置がやや
  ずれることがあります。
- **ファイル内検索はマッチ行単位**のハイライトです（行内の位置は強調されません）。
- **クリップボード（arboard）はローカル環境向け**。SSH 経由での利用では
  端末クリップボードに届かないことがあります。

---

## ライセンス

このプロジェクトは **MIT OR Apache-2.0** のデュアルライセンスです。
詳細は [`LICENSE-MIT`](LICENSE-MIT) および [`LICENSE-APACHE`](LICENSE-APACHE) を参照してください。

第三者コンポーネントのライセンスは [`THIRD-PARTY-NOTICES.md`](THIRD-PARTY-NOTICES.md) を参照してください。
特記すべき点：

- **libgit2**（git2 クレートで vendored 静的リンク）：GPLv2 + linking exception
- **nucleo-matcher**：MPL-2.0
